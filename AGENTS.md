# AGENTS.md · wx-audio-studio 开发规范

> 本文件是本仓库的**最高优先级开发约定**，人类开发者与 AI 编码助手（agent）共同遵守。
> 最后更新：2026-09-12 ｜ 状态：生效中 ｜ 变更本文件需在 `docs/CHANGELOG.md` 登记。
>
> **本文负责**：流程与规范（文档先行、分层强制、编码约定、性能红线、提交与 DoD、合规红线）。
> **本文不负责**：任何设计与实现细节。那些一律在 `docs/` 内，本文只引用而不复制。
> **同一信息只在一处定义**：本文与 `docs/` 出现重叠时，先确认归属，只修一处。

---

## 0. 第一原则：文档先行，代码同步（Doc-First）

**本项目是文档驱动开发。任何改动先落文档，再写代码；代码与文档必须始终一致。**

### 0.1 标准改动流程

```
① 提案（说明要改什么、为什么）
        ↓
② 改文档（docs/ 下对应文档 + 标注状态）
        ↓
③ 定稿（关键决策需确认；小改动可直接定稿）
        ↓
④ 写代码（严格按文档实现，不夹带文档外的设计）
        ↓
⑤ 测试（单测 + 真机验证）
        ↓
⑥ 回填文档（Spike 结论、实测数据、实现中发现的偏差）
        ↓
⑦ 提交（代码 + 文档在同一次提交内）
        ↓
⑧ 在 docs/CHANGELOG.md 登记一行（文档级变更）
```

### 0.2 改动与文档同步矩阵（提交前必须逐条对照检查）

| 你改了什么 | 必须同步更新的文档 |
| --- | --- |
| 新增/删除/调整功能或优先级 | `docs/01-product-spec.md`（功能清单表、MVP 边界） |
| 使用任何新的 `wx.*` 平台 API | `docs/02-platform-capability.md`（能力表 + 版本 + 降级策略） |
| 更换技术方案 / 引入新依赖 / 引入新库 | `docs/02-platform-capability.md`（选型决策 + 被否方案理由） |
| 跑完一项 Spike 验证 | `docs/02-platform-capability.md`（回填结论与实测数据，勾掉待验证项） |
| 发现或关闭一个风险 | `docs/02-platform-capability.md`（风险登记表） |
| 修改中间格式 / WAV 结构 / 峰值结构 | `docs/03-audio-engine.md`（第 2、4 节），必要时加 schema 版本迁移 |
| 新增或修改 DSP 算法 / 参数含义 / 默认值 | `docs/03-audio-engine.md`（第 6 节），含数学依据与参数范围 |
| 修改效果链顺序、渲染调度、模块接口签名 | `docs/03-audio-engine.md`（第 5、6.6、8 节） |
| 修改播放/试听策略、缓存失效规则 | `docs/03-audio-engine.md`（第 7 节） |
| 修改页面结构、布局、手势、文案规范 | `docs/04-ui-ux.md` |
| 新增/修改组件及其对外属性 | `docs/04-ui-ux.md`（组件清单）+ `docs/03`（若涉及引擎契约） |
| 修改 EDL 数据结构 / 字段语义 | `docs/05-data-model.md` + **`schemaVersion` 迁移函数 + 迁移单测** |
| 修改存储路径 / 命名 / 清理策略 | `docs/05-data-model.md`（第 4、5 节） |
| 修改撤销重做机制 | `docs/05-data-model.md`（第 6 节） |
| 调整目录结构 / 工具链 / 里程碑 | `docs/06-engineering-roadmap.md` |
| 修改性能或内存预算 | `docs/06-engineering-roadmap.md`（第 4 节） |
| 任何涉及隐私、权限、第三方许可的改动 | `docs/06-engineering-roadmap.md`（第 5 节合规） |
| 新增一条不可逆的架构决策 | 新建 `docs/adr/NNNN-简述.md`（见 0.5） |
| 以上任意文档变更 | `docs/CHANGELOG.md` 登记一行 |

**提交前必须自查这张表。** 凡是"改了但没同步文档"的改动，视为未完成（见第 12 节 DoD）。

### 0.3 例外：紧急修复

允许"先改代码后补文档"，但必须满足：
1. 属于修复崩溃、数据损坏、审核驳回等紧急问题；
2. **在同一个工作日的下一次提交内**补齐文档（不得跨版本）；
3. 在 `docs/CHANGELOG.md` 中以 `[补记]` 标注，说明为何走了例外。

除此之外没有例外。**"代码写完了再补文档"不是例外，是违规。**

### 0.4 文档元信息头（每份文档必须有）

每份文档第一行标题下方必须有以下元信息行，且随改动更新日期：

```markdown
# XX · 标题

> 状态：**已定稿** ｜ 最后更新：2026-09-12 ｜ 关联代码：`miniprogram/core/xxx.ts`
```

状态取值（只允许这四个）：`草案` / `已定稿` / `实现中` / `已过时`。
- `草案`：设计未确认，不得据此写代码。
- `已定稿`：可据此实现；实现中发现偏差 → 立即回到 `草案` 或直接改文档后再继续。
- `实现中`：代码正在按此文档落地。
- `已过时`：已被新文档/新决策取代，必须写明取代者。

**未实现的功能，元信息中的"关联代码"写"未实现（设计阶段）"**，实现后回填真实路径。

### 0.5 架构决策记录（ADR）

凡是满足以下任一条的决策，必须在 `docs/adr/` 下新建 `NNNN-短标题.md`：
- 一旦定下就很难改（数据结构、存储格式、导出格式）
- 有多种可行方案且我们主动放弃了其中一些
- 与平台限制直接相关（如"因为没有 OfflineAudioContext 所以自研渲染"）

ADR 模板（固定五段）：

```markdown
# ADR-NNNN：决策标题
- 状态：已接受 ｜ 日期：YYYY-MM-DD ｜ 关联：docs/03-audio-engine.md
## 背景
## 候选方案
## 决策
## 后果（正面 / 负面 / 需要在代码里注意的约束）
```

### 0.6 文档冲突处理

若代码与文档不一致，**以文档为准**，并立即在同一个提交内二选一：
- 修代码使其符合文档（首选）；
- 或先改文档说明新方案，再改代码。

不允许"代码是对的，文档先不管"的中间状态长期存在。

### 0.7 单一权威来源（SSOT）

**规则：每类信息只在一个文档里「定义」，其他文档只能「引用」。**

- **定义** = 给出完整内容、数值、公式、字段、表格、理由（含取舍论证）。
- **引用** = 一句话概述 + 指向权威处的链接。**不得展开论证、不得复制表格、不得另给一套数值。**

#### SSOT 归属表（信息 → 唯一权威文档）

| 信息类别 | 唯一权威处 | 典型引用者 |
| --- | --- | --- |
| 用户 / 场景 / 功能清单与优先级 / MVP 边界 / 产品指标 | `docs/01` | 04、06 |
| 平台 API 事实（接口、版本、限制、格式支持） | `docs/02 §1` | 03、04、05、06 |
| 技术选型、被否方案、容量估算与内存推导 | `docs/02 §2–§3` | 03、06 |
| 风险登记（影响 / 概率 / 缓解） | `docs/02 §4` | 06 |
| Spike 清单（问题、方法、通过标准） | `docs/02 §5` | 06、CHANGELOG |
| 中间格式 / 峰值结构 / 渲染调度 / DSP 算法与参数 / 播放策略 / 模块接口 | `docs/03` | 04、05、本文 |
| 页面结构 / 布局 / 手势 / 组件契约 / 视觉规范 | `docs/04` | — |
| EDL 类型与字段 / 存储布局与命名 / 撤销重做 / 迁移 | `docs/05` | 03、04 |
| 代码目录结构 / 工具链 / 里程碑与功能验收 / 性能指标口径 / 合规 | `docs/06` | 01、02、本文 |
| 分层规则的强制力 / 编码规范 / 单位后缀 / 提交流程 / 测试规范 / 红线 / DoD | `AGENTS.md`（本文） | `docs/*` |
| 文档变更史 / Spike 状态跟踪 | `docs/CHANGELOG.md` | — |
| 索引、术语速查、阅读顺序 | `docs/README.md` | — |

#### 执行要求

1. **新增信息前先判断归属**：对照上表找到唯一文档写进去；其他文档需要它时写链接。
2. **发现重复时的处理**：保留论证最完整的那一处作为权威，其余压缩为「一句话 + 链接」；若两处内容不一致，先确认哪个正确（不确定时问用户），再改一处、删另一处。
3. **禁止**：同一张表、同一组数值、同一段论证出现在两个文档里；用"详见 X"包一转又把原文抄过去。
4. **引用可以带结论数值**（如"内存峰值 ≤ 120MB，定义见 06 §4"），但**不得展开理由**，且必须带链接。
5. `docs/README.md` 的速览表**不算定义**，它是导航索引：必须逐行标注权威文档，且不写论证。
6. **章节标题只写标题本身**，不加附加说明（如"（唯一权威处）"）—— 说明放正文首句。标题一变，全库跳转锚点会静默失效。
7. 各文档头部的「本文负责 / 本文不负责」声明是该文档的边界声明，**必须与上表一致**；边界调整时两处同时改。

---

## 1. 目录与分层硬性规则

> **本节是分层规则的唯一权威处。** 架构视角的目录规划与依赖映射见 [docs/06 §1](./docs/06-engineering-roadmap.md#1-目录结构规划)，但规则的强制力与检查方式以本节为准。

| 规则 | 原因 | 违反后果 |
| --- | --- | --- |
| `core/dsp/**`、`core/audio/**`、`core/peaks/**`、`core/edl/**` **禁止 import 任何 `wx.*`** | 必须能在 Node 中单测 | 单测无法运行 |
| `core/engine/render.ts` 只依赖 `core/dsp`、`core/edl` 与 TypedArray | Worker 可用 API 白名单未知（DB-05） | 渲染在真机上不可用 |
| 所有 EDL 变更**只能**通过 `core/edl/ops.ts` | 集中做 clamp、排序、帧对齐 | 时间轴越界/重叠类 bug |
| 跨线程通信的消息类型**只能**定义在 `core/engine/worker-protocol.ts` | 类型安全 + 单一来源 | 主线程与 Worker 协议漂移 |
| 平台耦合**只能**出现在 `core/fs/**`、`core/player/**`、`core/caps.ts`、`pages/**`、`components/**` | 隔离平台依赖 | 无法测试 |
| UI 层不得直接调用 `FileSystemManager` / `WebAudioContext` | 统一走 `core/` 封装 | 容量统计与错误处理失控 |
| 不得硬编码 `USER_DATA_PATH` 或任何文件路径字符串 | 统一走 `core/fs/paths.ts` | 路径散落、清理策略失效 |

用 ESLint `no-restricted-imports` + `no-restricted-globals` 强制前两条，代码审查强制其余。

## 2. 编码规范

### 2.1 TypeScript

- `strict: true`，且开启 `noUncheckedIndexedAccess`、`noImplicitOverride`。
- **禁止 `any`**。确需逃生舱时用 `unknown` + 类型守卫；万不得已用 `any` 必须写 `// eslint-disable-next-line` + 一行理由注释。
- 公共 API（`core/**` 导出）必须有显式返回类型。
- 禁止非空断言 `!`（除 DOM/SelectorQuery 查询结果，且需紧邻一次判空）。
- 使用 `??`、`?.` 而非 `||`、多层 `if`。注意 `||` 会把 `0` 和 `''` 吞掉 —— 音频参数里 `0` 是合法值（0dB、0 声像），**这是高危错误**。

### 2.2 命名

| 对象 | 规范 | 示例 |
| --- | --- | --- |
| 文件 | `kebab-case` | `wav-file.ts`、`noise-gate.ts` |
| 组件目录 | `kebab-case` 目录 + 同名文件 | `components/waveform-canvas/` |
| 类型 / 接口 / 枚举 | `PascalCase` | `EffectInstance`、`FadeSpec` |
| 函数 / 变量 | `camelCase` | `applyFadeIn`、`peaksRef` |
| 常量 | `UPPER_SNAKE_CASE` | `BASE_BUCKET`、`MAX_HISTORY` |
| 布尔量 | `is/has/can/should` 前缀 | `isDirty`、`hasSelection` |
| 事件处理 | `onXxx`（组件内）/ `handleXxx`（页面内） | `onGestureChange` |
| 消息类型 | `SCREAMING_SNAKE` 字符串联合 | `'chunkDone'`、`'assetRequest'` |

### 2.3 单位后缀（**音频项目的命脉，必须严格执行**）

数值型变量、参数、字段**必须带单位后缀**，禁止裸数字表达物理量：

| 后缀 | 含义 | 示例 |
| --- | --- | --- |
| `Sec` / `Ms` | 秒 / 毫秒 | `durationSec`、`attackMs` |
| `Db` | 分贝（对数） | `gainDb`、`thresholdDb` |
| `Linear` | 线性增益（0～1+） | `gainLinear` |
| `Samples` / `Frames` | 采样点 / 采样帧 | `chunkSamples`、`frames` |
| `Px` | 像素 | `pxPerSecond`、`handleWidthPx` |
| `Bytes` | 字节 | `dataBytes` |
| `Ratio` | 比例（0～1 或倍数） | `speedRatio` |
| `Hz` / `Bps` | 赫兹 / 比特率 | `sampleRateHz`（内部可用 `sampleRate`） |

**强制规则**：
- 分贝与线性增益**永不混用**。函数名或参数名必须表明：`dbToLinear()`、`applyGain(buf, gainLinear)`。
- 数据模型与 EDL 中的时间**一律用秒（float）**；采样帧只出现在 DSP 与渲染内部，且转换处必须显式写 `Math.round(t * sampleRate)`。
- 禁止在函数内部隐式假设采样率或声道数：必须由参数传入，或来自显式传入的 `AudioFormat`。

### 2.4 注释

- 对外导出的函数必须写 JSDoc：用途、参数单位、取值范围、副作用、是否就地修改。
- **DSP 算法必须写明数学依据与参考来源**（如 `// RBJ Audio EQ Cookbook, peaking EQ`），并给出关键公式。
- 就地修改（in-place）必须在函数名或 JSDoc 中显式标注（如 `xxxInPlace`），禁止悄悄改写调用方的数组。
- 不要写"// 循环遍历数组"这类复述代码的注释。
- 数值常量必须解释来源（如 `// -0.3dBFS：留出编码余量，避免转 16bit 后削波`）。

### 2.5 常量与魔法数字

- 任何出现两次以上的字面量必须提取为命名常量。
- 算法参数默认值统一放在各模块顶部的 `const DEFAULTS = {...}`，禁止散落在函数体内。
- 与产品/性能相关的阈值（时长上限、内存预算、分块大小）必须集中在 `core/constants.ts` 并注明文档出处（如 `// 见 docs/03 第 5.4 节`）。

## 3. 音频与 DSP 专项规范

1. **纯函数优先**：`core/dsp/**` 里所有函数无副作用、不读写全局、不访问平台 API；输入输出都是 `Float32Array` / `Int16Array` / 标量。
2. **数值安全**：入口处对输入做 clamp 与 `Number.isFinite` 检查；滤波器反馈路径加极小量（如 `1e-20`）防 denormal；浮点输出累加后统一 clamp 到 `[-1, 1]` 再转 Int16。
3. **不做逐采样内存分配**：循环内禁止 `new`、禁止创建闭包、避免数组 `push`；缓冲一律预分配复用。
4. **避免循环内 `Math.pow` / `Math.exp`**：需要连续包络时用增量乘法（如 `gain *= ratioPerSample`）或预先算好系数。
5. **效果链顺序不可随意调换**：必须遵守 `docs/03-audio-engine.md` 第 6.6 节的固定管线（片段 → 轨道 → 总线）。若需要改变顺序，先改文档 + ADR。
6. **采样率/声道必须是显式输入**：禁止模块级默认采样率常量被隐式使用。
7. **可听性优先于理论完美**：先用合成信号验证数值正确，再用真机听感验证；两者冲突时以听感为准并在文档中记录取舍。
8. **每个 DSP 函数必须有对应单测**，包含：正常输入、全零输入、极值输入（±1 满量程）、长度 < 窗长的短输入。
9. **性能基准**：任何 O(n) 以上的算法（WSOLA、谱减、FFT）必须在 `tests/bench/` 留一条基准脚本，记录 3 分钟音频的处理耗时；性能回归超过 20% 视为缺陷。

## 4. 平台 API 使用规范

1. **登记制**：使用任何 `wx.*` API 前，确认它已登记在 `docs/02-platform-capability.md` 的能力表中。**不允许使用未登记的 API**（防止隐性平台依赖）。
2. **三件套齐全**才能用：① 官方最低基础库版本；② 失败/不支持的降级路径；③ 能力探测代码（写入 `core/caps.ts`）。
3. **真机优先**：任何新平台 API 首次引入，必须先在 `spikes/` 中验证（见 `docs/02` 的 Spike 清单，新增项要编号 `DB-xx`）并在文档中回填结论，然后才能在业务代码中使用。
4. **PC 与移动的差异要显式处理**：如 `sampleRate` 在 PC 不支持设置、`saveFileToDisk` 仅 PC 可用。
5. **异步 API 必须处理 fail 分支**，禁止只写 `success`。
6. **音频中断必须处理**：`wx.onAudioInterruptionBegin/End`、页面 `onHide` 的暂停与恢复。
7. **播放器实例单例**：`createInnerAudioContext` 不得在循环/频繁路径中创建；页面卸载必须 `destroy()`。

## 5. 性能与内存红线

**指标数值**（主线程单次任务、内存峰值、解码并发数、峰值数据体积、渲染块大小、消息频率、工程 JSON 体积、手势期间行为）**见 [docs/06 §4](./docs/06-engineering-roadmap.md#4-性能与内存预算)** —— 本节不复制该表，改指标只需改那一处。

不可协商的**行为红线**（与指标数值无关，永远成立）：

1. 禁止把完整 PCM 常驻内存（包括"临时缓存一下"）。
2. 禁止在 UI 事件回调里做同步重活（解码、重采样、大循环）。
3. 禁止逐采样跨线程通信、逐采样文件读写。
4. 禁止在手势进行中触发全量波形重绘、渲染或落盘。

绕过任何红线需要**豁免**：在 [docs/06 §4](./docs/06-engineering-roadmap.md#4-性能与内存预算) 登记豁免项、原因、责任人与复查时间，并在代码处加注释指向该记录。

## 6. 错误处理与日志

1. **每个失败路径都要有用户可读的文案 + 一个可执行动作**（重试 / 清理存储 / 降低采样率 / 换格式），禁止只 toast "操作失败"。
2. 错误分级：`fatal`（阻塞流程，必须提示）/ `recoverable`（可重试）/ `silent`（降级继续，记日志）。
3. 禁止空 `catch {}`；禁止 `catch` 后只 `console.log` 就继续（要么处理，要么向上抛）。
4. **禁止裸 `console.log` 进入提交**。统一用 `utils/logger.ts`（`debug/info/warn/error`，按环境与开关输出）。
5. **日志不得包含用户音频内容、文件路径全路径、任何可识别信息**（隐私红线）。
6. 平台错误码要翻译：如 `1300202` → "存储空间不足"（见 `docs/02` 第 1.4 节的错误码表）。

## 7. 测试规范

**测试层次、断言方法、覆盖目标的权威定义见 [docs/06 §2.1 测试策略](./docs/06-engineering-roadmap.md#21-测试策略)。**

本节只定**不可协商的强制要求**：

1. `core/**` 的任何改动必须带单测；DSP / EDL / 峰值 / 渲染为强制覆盖区。
2. 平台相关行为必须真机验证，不得以开发者工具结果替代，结论回填 `docs/02`。
3. 提交前 `npm run check`（typecheck + lint + test）必须全绿。
4. **不得为了测试通过而弱化断言或删除测试**；测试与文档冲突时，先改文档再改测试。

## 8. 提交规范

### 8.1 提交信息（Conventional Commits）

```
<type>(<scope>): <简述>

<body：为什么改，而不是改了什么>
<关联：Refs docs/03-audio-engine.md#5.2>
```

| type | 用途 |
| --- | --- |
| `feat` | 新功能 |
| `fix` | 修 bug |
| `docs` | 仅文档（含 Spike 结论回填） |
| `refactor` | 重构（不改行为） |
| `perf` | 性能优化 |
| `test` | 测试 |
| `spike` | 技术验证代码（可随后删除） |
| `chore` | 构建/工具/依赖 |

`scope` 用模块名：`dsp`、`engine`、`peaks`、`edl`、`editor`、`export`、`fs`、`docs` 等。

### 8.2 提交粒度

- 一次提交只做一件事。禁止 "feat: 完成编辑器" 这类巨型提交。
- **代码与其文档改动必须同一次提交**（Doc-First 的核心落地）。若分两次，第二次必须在同一工作日内完成，并在提交信息中注明 `Refs`。

### 8.3 提交前检查清单（**逐项确认，缺一不可**）

```
[ ] 改动 → 文档同步矩阵（第 0.2 节）已逐条核对，对应文档已更新
[ ] 涉及数据结构变更时，schemaVersion 已升 + 迁移函数 + 迁移单测已写
[ ] 涉及新平台 API 时，docs/02 已登记（版本/降级/探测）且 spike 结论已回填
[ ] npm run check 全绿（typecheck + lint + test）
[ ] 新增/修改的 DSP 函数有单测，含极值与短输入
[ ] 无裸 console.log、无 any（或已注释理由）、无新增魔法数字
[ ] 无硬编码路径；音频/时间/增益数值都带单位后缀
[ ] 没有把 PCM 常驻内存、没有在热路径做 I/O
[ ] 未引入重复定义：新内容已归入 §0.7 归属表中的唯一权威文档，其他位置只加链接
[ ] 已更新 docs/CHANGELOG.md（文档级变更登记一行）
```

## 9. 隐私与合规红线（不可协商）

**具体合规要求、落地动作与提审自查清单见 [docs/06 §5 合规与审核](./docs/06-engineering-roadmap.md#5-合规与审核)** —— 本节只定红线：

1. **音频数据绝不上传**（含埋点、第三方 SDK）。全端上处理是产品承诺，任何上传逻辑一律禁止。
2. **不得内置未授权音乐/音效素材**；确需内置时必须有可商用授权凭证并登记在 `docs/06 §5`。
3. **权限与隐私**：麦克风用途须在《用户隐私保护指引》声明，调用前有明确提示，拒绝后有引导路径；隐私政策文本可访问且与实际行为一致。
4. **引入第三方库**（含 `lamejs`）须先评估许可证并登记，且不得与"全端上"承诺冲突。
5. **日志与上报不得携带可识别信息**。

## 10. 文档编写规范

1. 文件名：`docs/NN-kebab-case.md`（编号递增），ADR 为 `docs/adr/NNNN-标题.md`。
2. 交叉引用用**相对路径链接**，指向具体小节（如 `./03-audio-engine.md#52-渲染调度模型`）；改标题必须同步修链接（提交前跑一次链接校验）。标题书写规范见 [§0.7](#07-单一权威来源ssot)。
3. 每份文档头部有元信息行（第 0.4 节）；结尾如有"未决问题"章节，解决后必须删除或标记为已决。
4. 表格优先于长段落；决策要写"理由"，不只写"结论"。
5. 未验证的推断必须显式标注 `⚠️ 待验证` 并归入 `docs/02` 的 Spike 清单，**禁止把猜测写成事实**。
6. 平台相关的陈述必须附官方来源（接口名 + 版本 + 文档链接），禁止引用不可靠的二手博客作为唯一依据。
7. 数字与预算（内存、耗时、上限）必须标明单位与前提条件（设备、采样率、时长）。

## 11. 定义完成（DoD）

一个任务只有全部满足才算完成：

```
[ ] 文档已先行更新并定稿（状态标记正确）
[ ] 代码实现与文档一致；无文档外的"顺手设计"
[ ] 单测通过且覆盖达标；DSP/EDL 有对应数值与不变量测试
[ ] 真机验证通过（涉及平台能力时），结论已回填文档
[ ] 性能与内存红线未被突破（或已登记豁免）
[ ] 错误路径有用户可读文案与可执行动作
[ ] 元信息头日期、关联代码路径、CHANGELOG 均已更新
[ ] 无重复定义：新信息只写在 §0.7 归属表指定的文档，其余位置为链接
[ ] 提交信息符合规范，代码与文档在同一次提交
```

## 12. 禁止事项速查

> 每条的依据见所指小节 —— **解释不在此重复**，发现规则需要调整时改对应小节。

| 禁止 | 依据 |
| --- | --- |
| ❌ 先写代码后补文档 | [§0.3](#03-例外紧急修复) |
| ❌ 在 `core/dsp`/`core/audio` 里 import `wx.*`；UI 层写 DSP 或直接操作文件系统；硬编码路径 | [§1](#1-目录与分层硬性规则) |
| ❌ 用 `||` 判断音频参数（吞掉 `0dB`/`0` 声像）；dB 与线性增益混用；数值不带单位后缀 | [§2.3](#23-单位后缀音频项目的命脉必须严格执行) |
| ❌ 未登记的 `wx.*` API；不处理异步 fail 分支；频繁创建播放器实例 | [§4](#4-平台-api-使用规范) |
| ❌ PCM 常驻内存；并行解码多个音频；热路径做 I/O 或大循环 | [§5](#5-性能与内存红线) |
| ❌ 空 catch；只 `console.log` 就继续；裸 `console.log` 入库；日志带可识别信息 | [§6](#6-错误处理与日志) |
| ❌ 为通过测试而弱化断言 | [§7](#7-测试规范) |
| ❌ 编辑操作修改素材文件 | [§1](#1-目录与分层硬性规则) + [docs/03 §1.1](./docs/03-audio-engine.md#11-三条不变量) |
| ❌ 同一信息（表 / 数值 / 论证）在两个文档里重复定义 | [§0.7](#07-单一权威来源ssot) |
| ❌ 章节标题带附加说明（会弄坏跳转锚点） | [§0.7](#07-单一权威来源ssot) |
| ❌ 上传音频或引入任何上传逻辑；内置未授权素材 | [§9](#9-隐私与合规红线不可协商) |

## 13. 给 AI 编码助手（agent）的指令

1. **开始任何实现前**：先读 `docs/README.md` → `docs/01` → 与任务相关的文档（引擎任务读 `03`，数据任务读 `05`，界面任务读 `04`）。
2. **先改文档再改代码**。若任务描述与文档冲突，**停下来提问**，不要自行选一个方向实现。
3. 若文档中某项标注 `草案` 或 `⚠️ 待验证`，**不得**据此写业务代码；先提议做 Spike 或请用户确认。
4. 遵守第 1 节的分层规则与第 2.3 节的单位后缀规则 —— 这两处最容易出错。
5. 不要引入文档未提及的依赖、库、平台 API。需要引入时，先更新 `docs/02` 并在回复中说明理由。
6. 每完成一处改动，对照第 0.2 节的同步矩阵更新文档，并登记 `docs/CHANGELOG.md`。
7. 不要"顺手重构"未在任务范围内的文件。
8. 汇报时说明：改了哪些文件（含文档）、为什么、还有哪些未决问题。
9. **新增内容前先对照 [§0.7 SSOT 归属表](#07-单一权威来源ssot) 确定唯一权威文档**；不要把同一信息写进多个文档，也不要擅自在非权威文档里"补充说明"。

---

## 附：相关文档

- [docs/README.md](./docs/README.md) — 文档索引与项目速览
- [docs/01-product-spec.md](./docs/01-product-spec.md) — 功能范围与优先级
- [docs/02-platform-capability.md](./docs/02-platform-capability.md) — 平台能力、选型、Spike 清单
- [docs/03-audio-engine.md](./docs/03-audio-engine.md) — 引擎架构与 DSP
- [docs/04-ui-ux.md](./docs/04-ui-ux.md) — 交互与视觉
- [docs/05-data-model.md](./docs/05-data-model.md) — 数据模型与存储
- [docs/06-engineering-roadmap.md](./docs/06-engineering-roadmap.md) — 工程、里程碑、合规
- [docs/CHANGELOG.md](./docs/CHANGELOG.md) — 文档变更记录
