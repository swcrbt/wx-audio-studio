{
  "name": "wx-audio-studio",
  "version": "0.1.0",
  "private": true,
  "description": "微信小程序音频编辑器：录音 · 波形剪辑 · 拼接混音 · 音效 · 导出",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench --run",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "miniprogram-api-typings": "^5.2.3",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.12.0",
    "vitest": "^2.1.4"
  }
}
